# 🚀 Ending Forecast Dashboard

Next.js로 구축된 **3541행 대용량** ending focast.xlsx 데이터 기반 실시간 영업 대시보드입니다.

## ✨ 주요 기능

### 📊 대시보드 기능
- **3개 탭 인터페이스**: 개요, 예측, 상세 데이터
- **실시간 KPI**: 총 매출, 목표 달성률, 신규 고객, 주문 건수
- **인터랙티브 차트**: 월별 매출, 지역별 성과, 실적 vs 예측
- **고급 데이터 테이블**: 검색, 정렬, 페이지네이션
- **자동 새로고침**: 5분마다 데이터 갱신

### 📈 데이터 분석
- **엑셀 자동 연동**: ending focast.xlsx 파일 실시간 반영
- **대용량 처리**: 3541행 데이터 최적화 처리
- **스마트 매핑**: 다양한 컬럼명 자동 인식
- **Forecast 분석**: 예측 vs 실적 비교 및 시각화

### 🎨 UI/UX
- **모던 디자인**: 그라디언트 배경, 부드러운 애니메이션
- **반응형**: 모바일/태블릿/데스크톱 완벽 지원
- **직관적 네비게이션**: 탭 기반 정보 구조
- **실시간 피드백**: 로딩 상태, 오류 처리

## 🚀 빠른 시작

### 방법 1: 자동 설정 (추천) ⭐

**`setup-excel.bat` 파일을 더블클릭**하면 자동으로:
1. ✅ xlsx 패키지 설치
2. ✅ ending focast.xlsx 파일 분석
3. ✅ 데이터 구조 확인
4. ✅ JSON 분석 파일 생성

그 다음:
```bash
npm run dev
```

### 방법 2: 수동 설정

```bash
# 1. 패키지 설치
npm install
npm install xlsx

# 2. 엑셀 파일 분석 (선택사항)
npm run analyze

# 3. 개발 서버 실행
npm run dev

# 4. 브라우저에서 확인
# http://localhost:3000
```

### 프로덕션 빌드

```bash
npm run build
npm start
```

## Vercel 배포

이 프로젝트는 Vercel에 배포하기 최적화되어 있습니다:

1. GitHub 저장소에 코드를 푸시합니다
2. [Vercel](https://vercel.com)에 로그인합니다
3. "New Project"를 클릭하고 저장소를 선택합니다
4. 자동으로 빌드 및 배포됩니다

## 🛠️ 기술 스택

- **Framework**: Next.js 14 (App Router)
- **Language**: TypeScript
- **Styling**: Tailwind CSS
- **Charts**: Recharts (라인, 바, 컴포지트 차트)
- **Icons**: Lucide React
- **Data**: xlsx (엑셀 파일 처리)
- **State**: React Hooks

## 📁 프로젝트 구조

```
├── app/
│   ├── api/
│   │   ├── dashboard/route.ts    # 메인 데이터 API
│   │   └── test-excel/route.ts   # 엑셀 테스트 API
│   ├── layout.tsx                 # 루트 레이아웃
│   ├── page.tsx                   # 메인 페이지
│   └── globals.css                # 글로벌 스타일
├── components/
│   ├── EnhancedDashboard.tsx     # 🆕 업그레이드된 대시보드
│   ├── ForecastChart.tsx         # 🆕 예측 차트
│   ├── DataTable.tsx             # 🆕 고급 테이블
│   ├── MetricCard.tsx            # 🆕 메트릭 카드
│   ├── KPICard.tsx               # KPI 카드
│   ├── SalesChart.tsx            # 매출 차트
│   ├── TargetChart.tsx           # 목표 달성률 차트
│   └── RecentSales.tsx           # 최근 판매 테이블
├── lib/
│   ├── excelReader.ts            # 엑셀 파일 읽기
│   └── utils.ts                  # 유틸리티
├── types/
│   ├── dashboard.ts              # 대시보드 타입
│   └── forecast.ts               # 🆕 예측 타입
├── ending focast.xlsx            # 📊 데이터 소스 (3541행)
├── setup-excel.bat               # 🆕 자동 설정 스크립트
├── analyze-excel.js              # 🆕 엑셀 분석 스크립트
└── package.json
```

## 커스터마이징

### 데이터 변경

각 컴포넌트 내의 데이터 배열을 수정하여 실제 데이터로 교체할 수 있습니다:

- `components/SalesChart.tsx` - 월별 매출 데이터
- `components/TargetChart.tsx` - 지역별 달성률 데이터
- `components/RecentSales.tsx` - 최근 판매 내역

### API 연동

실제 API와 연동하려면 `Dashboard.tsx`의 `useEffect`에서 데이터를 fetch하세요:

```typescript
useEffect(() => {
  async function fetchData() {
    const response = await fetch('/api/dashboard');
    const data = await response.json();
    // 데이터 처리...
  }
  fetchData();
}, []);
```

## 라이선스

MIT

